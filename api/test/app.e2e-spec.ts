/**
 * Stub antigo do scaffold Nest — substituído por painel.e2e-spec.ts
 * Mantido vazio de casos reais para não falhar o runner.
 */
describe('scaffold legado', () => {
  it('existe só para não quebrar imports antigos', () => {
    expect(true).toBe(true);
  });
});
